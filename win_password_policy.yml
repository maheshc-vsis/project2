---
- name: Password Policy
  hosts: all
  collections:
    - name: community.windows
  tasks:
    - name: Ensure Enforce password history
      community.windows.win_security_policy:
        sction: System Access
        key: PasswordHistorySize
        value: "{{ max_passwords_saved }}"
